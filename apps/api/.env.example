# API server
PORT=3000
CLIENT_ORIGIN=http://localhost:4200

# Auth
JWT_SECRET=your-secret-here
SUPER_ADMIN_EMAILS=admin@example.com
# Public POST /auth/register: set true to allow, false to forbid. Unset = allowed only when NODE_ENV !== production.
ALLOW_PUBLIC_REGISTER=true

# Database
MONGO_URI=mongodb://localhost:27017/saas-booking
